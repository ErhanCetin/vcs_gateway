"""
Example unit test — replace with actual service tests.

Unit tests:
- No real DB, Redis, or RabbitMQ
- Mock all external dependencies
- Test business logic only
- Fast — should complete in milliseconds
"""

import pytest


@pytest.mark.unit
def test_example_placeholder() -> None:
    """Replace this with actual unit tests for your service logic."""
    assert 1 + 1 == 2
